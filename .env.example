# Non-secret config only. Real secrets live in Azure Key Vault (see scripts/post_deploy.sh).
# Copy to .env for local overrides.

# Local pipeline
DATA_DIR=data
SEED=42

# Azure (used by scripts/*.sh; the .bicepparam file is the source of truth for deploys)
AZURE_LOCATION=eastus
RESOURCE_PREFIX=dtbi
